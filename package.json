{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "predeploy": "npm run build"  // 新增：部署前自动构建，确保依赖完整
  },
  "dependencies": {
    "next": "14.2.10",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "eslint": "8.57.0",
    "eslint-config-next": "14.2.10",
    "http-proxy-middleware": "^3.0.2"
  },
  "engines": {
    "node": ">=18.x"  // 新增：指定Node.js版本，与Vercel默认环境匹配
  }
}
